class MaxPooling:
    def __init__(self, step=(2, 2), size=(2, 2)):
        if type(step) and type(size) == tuple:
            self.step = step
            self.tuple = tuple

    def __call__(self, *args, **kwargs):
        pass

